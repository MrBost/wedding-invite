spring:
  application:
    name: wedding-invite
  config:
    import: optional:file:.env[.properties]
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://dpg-d45emmu3jp1c73dfqdf0-a.oregon-postgres.render.com/rsvp_u5d2
    #    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:rsvp}?useEncoding=true&characterEncoding=UTF-8
#    hikari:
    username: ${DB_USER_NAME:postgres}
    password: ${DB_PASSWORD:1234}
    driver-class-name: org.postgresql.Driver

server:
  port: ${SERVER_PORT:2714}

wedding:
  invitation:
    base-url: ${INVITE_URL:https://wedding-invite-zxks.onrender.com}
    card:
      template-path: ./templates
      output-path: ./generated-cards
    venue:
      max-guests: 120
      seats-per-table: 10

logging:
  level:
    com.wedding.invitation: DEBUG
